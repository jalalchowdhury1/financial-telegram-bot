# yaml-language-server: $schema=https://json.schemastore.org/github-workflow
name: CI

# Pre-merge gate: nothing ran on PRs before, so "CI must pass to merge" wasn't actually
# enforced. This runs the backend + dashboard test suites on every PR to main, which is
# also the gate the Phase 2 self-improve agent's PRs must clear.
on:
  pull_request:
    branches: [main]

jobs:
  backend-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: 'pip'
      - name: Install deps
        run: pip install -r requirements-dev.txt
      - name: pytest
        run: python -m pytest tests/ -q

  dashboard-tests:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: dashboard
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: dashboard/package-lock.json
      - name: Install
        run: npm ci
      - name: Test
        run: npm test -- --watchAll=false
      - name: Build
        run: npm run build
